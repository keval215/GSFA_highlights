---
name: supermemory_index
description: Maps docs/ sections to verbatim Supermemory content under containerTag sm_project_gsfa, plus the last-synced commit SHA for each. Consult before saving/forgetting so /update-doc replaces facts precisely instead of duplicating them.
type: reference
---

containerTag for every row below: `sm_project_gsfa`

**Known limitation (learned 2026-07-07, do not re-litigate):** `mcp__supermemory__memory`'s
`forget` action only matches against a document's small, auto-generated `memoryEntries`
(atomic one-sentence facts Supermemory derives asynchronously) — never the raw saved
`content` string, and never the auto-generated `summary`. Both were tried directly
against real saved documents and failed ("No matching memory found to forget... only
document chunks matched"). Several documents never get decomposed into `memoryEntries`
at all (empty array), making them **permanently un-forgettable by content** via this
tool. Consequence: **do not rely on `forget` to remove a stale document.** Always
attempt it (cheap, sometimes works if entries exist), but treat this index — not
Supermemory's own state — as the source of truth for "what's canonical." If `forget`
reports success, mark the row's "Forget attempted" cell `ok`; if it reports "no
matching memory," mark it `stale-orphan (harmless)` and leave the new row as the one
`recall()` consumers should trust. Never spend more than one retry on a failed forget.

| Doc file / section | Verbatim saved content (current) | Document id | Last-synced SHA | Forget attempted (prior version) |
|---|---|---|---|---|
| docs/codebase/README.md — scope/overview | "GSFA_highlights is a futsal/football match-video analysis system (repo at D:\GSFA_highlights). It derives per-team (team0/team1) possession %, completed passes, interceptions, and ball-lost events from video, producing an append-only event log. It does not yet edit/cut highlight reels — the events table is raw material for a future highlight-reel builder." | bbRq4ZfckPe8HznZTiWLE6 | 2ecc1eea3eff027bb1e683b42c010434a312f527 | n/a (first version) |
| docs/codebase/ARCHITECTURE.md §1,5 — per-frame pipeline chain | "GSFA_highlights per-frame pipeline chain (perception stage): PlayerDetector (unified YOLOv11m, emits active_player/ball/goal_post/referee classes) -> GSFATeamClassifier (SigLIP embeddings, team assignment) -> GoalkeeperDetector (GK + team fix). Then \"who has the ball\" stage: PlayerTracker (BoT-SORT) -> BallTracker (Kalman) -> CarrierEngine (foot-zone based) -> PassEventTracker (release/travel/receive FSM). Output fans out to PossessionStats (local mode, in-memory) or MinuteCounters (service mode, writes SQL minute_stats)." | oDNpxK7Rj6ToTEMaEdetYy | 2ecc1eea3eff027bb1e683b42c010434a312f527 | n/a (first version) |
| docs/codebase/README.md + ARCHITECTURE.md §2-3 — two execution modes | "GSFA_highlights has two execution modes sharing the same CV pipeline classes (PlayerDetector, GSFATeamClassifier, PlayerTracker, BallTracker, CarrierEngine, PassEventTracker). Mode A: local script `video_analysis/possession.py` -> run(), processes one whole video file on disk, renders an annotated mp4 + printed summary, is the reference/debugging implementation. Mode B: production service, `service/api.py` (FastAPI ingest, no GPU, accepts 60s clip uploads via POST /api/clips, also has a post_processing whole-match upload path) + `service/worker.py` (GPU, polls Azure Queue, runs the shared pipeline, writes Azure SQL rows + HTTP callbacks). Mode B never renders video." | 394J7qZojuupwHBtm3ZRfr | 2ecc1eea3eff027bb1e683b42c010434a312f527 | n/a (first version) |
| docs/codebase/ARCHITECTURE.md §4 / detectors/README.md — wire-format types | "GSFA_highlights shared wire-format types, defined in detectors/player_detector.py: Detection (bbox, confidence, foot_point, centre_point, plus mutable fields filled in later by other stages: team_id by TeamClassifier, is_goalkeeper by GoalkeeperDetector, track_id by PlayerTracker, embedding by SigLIP, smoothed_bbox by PlayerTracker's Kalman smoothing — drawing-only, raw bbox stays untouched) and FrameDetections (players/referees/goal_posts/balls/all). video_analysis/possession.py adds BallDetection and CarrierState (carrier/loose/oof). Every stage mutates Detection objects in place rather than returning new ones." | S2sTcnebsaUbYEJLFwL5tN | 2ecc1eea3eff027bb1e683b42c010434a312f527 | n/a (first version) |
| docs/codebase/service/README.md — worker clip-handling flow | "GSFA_highlights service-mode worker flow (service/worker.py Worker._handle, per queued clip): 1) poison guard (dequeue_count > MAX_DEQUEUE_COUNT), 2) idempotency check on (match_id, half, minute) primary key, 3) ordering guard via is_expected(), deferred/re-queued up to ORDERING_RETRIES then processed with a logged gap, 4) MatchSessionManager.get_or_create(match_id) returns cross-clip MatchSession, runs team fit on clip 1 (or quality-guard refit on clip 2), 5) clip_processor.process_clip() runs the shared CV pipeline at TARGET_PROCESS_FPS, 6) one SQL transaction writes minute row + prior-minute correction + events + match progress + outbox row (service/db.py, Azure SQL), 7) notifier.send_pending_for_match() POSTs cumulative stats in order via callback_outbox, 8) cleanup deletes queue message + blob + local job dir. service/stats.py mirrors possession.py's constant strings and asserts they match at import time." | YXrE8nSAnMmZNPsQAevtcd | 2ecc1eea3eff027bb1e683b42c010434a312f527 | n/a (first version) |
| docs/codebase/ARCHITECTURE.md §7 / service/README.md — correctness invariants | "GSFA_highlights correctness invariants: (1) Provisional credit + retroactive adjustment — during a pass's travel phase the passer's team is credited provisionally; if it becomes an interception or ball-lost, frames are corrected afterward via PossessionStats.apply_adjustments (local) or MinuteCounters.apply_adjustment + a PriorCorrection UPDATE when the pass spans a clip boundary (service). (2) Cumulative-on-read — minute_stats table stores only raw per-minute counters; cumulative totals are SUM()-ed at read time via db.cumulative_read, percentages derived after summing, never averaged. (3) Transactional outbox — the callback payload row is written in the same SQL transaction as the minute row. (4) Idempotency + ordering guards prevent duplicate/out-of-order processing." | ey78oW697x8dnGn64bKfQz | 2ecc1eea3eff027bb1e683b42c010434a312f527 | n/a (first version) |
| docs/codebase/ARCHITECTURE.md §6 / detectors/README.md — unified YOLOv11m change | "GSFA_highlights recent architectural change: the pipeline used to run two detection models per frame (YOLOv11 for players + RF-DETR for the ball); it now runs one unified YOLOv11m model emitting all four classes (active_player, ball, goal_post, referee), with best_ball() picking the ball from the same forward pass. The old separate RF-DETR ball model and weights were removed entirely. heatmap.py and shots_on_t.py are standalone/experimental and still use the older, separate detectors (out of scope for the unification)." | rUAiGrzk8eNNwkhpbGQMs9 | 2ecc1eea3eff027bb1e683b42c010434a312f527 | n/a (first version) |
| docs/codebase/README.md — repository map | "GSFA_highlights repository layout: detectors/ (YOLOv11m detection, GK logic, cache), team_classifier/ (GSFATeamClassifier SigLIP - production - and ColourHistogramTeamClassifier variant), tracking/ (BoT-SORT wrapper consuming external SigLIP embeddings), video_analysis/ (possession.py core pipeline + homography/keypoint tools), service/ (FastAPI ingest api.py + GPU worker.py + Azure + SQL: session.py, clip_processor.py, stats.py, db.py, notifier.py), sql/ (Azure SQL schema: matches, minute_stats, post_processing, events, callback_outbox), scripts/ (CSV export get_csv.py, clip upload upload_clips.ps1, team diagnostics), data/ (gitignored cache pkls + output), heatmap.py and shots_on_t.py (standalone experimental tools), Dockerfile + docker-compose.yml (single image for api+worker), .github/workflows (CI: push to main triggers SSH redeploy on Azure VM, retargeted from test branch to main)." | BHdWHu3P8byVqcXNqcmsD8 | 2ecc1eea3eff027bb1e683b42c010434a312f527 | n/a (first version) |
| docs/codebase/team_classifier/README.md — SigLIP vs colour-histogram decision | "GSFA_highlights team classifier decision: always use GSFATeamClassifier (SigLIP-based), never ColourHistogramTeamClassifier, per explicit user preference — SigLIP embeddings are the production default for team assignment." | 9ysW9hvucBfnoA6XwBBs3o | 2ecc1eea3eff027bb1e683b42c010434a312f527 | n/a (first version) |
| docs/codebase/infra/README.md — Docker/Python 3.11 constraint | "GSFA_highlights deployment constraint: Docker must use deadsnakes Python 3.11 — Ubuntu's default Python 3.11.0rc1 segfaults torch.jit.script inside rfdetr-related code paths." | iVhwjMCGRbFoJcBr8wzai7 | 2ecc1eea3eff027bb1e683b42c010434a312f527 | n/a (first version) |

**Migration note (2026-07-07):** these 10 documents were originally saved under
`containerTag: sm_project_default` (mixed with one unrelated Deep-EIoU/GTA-Link
tracking memory from a different exploration, id `AQLYnPpvxjY6RWQ92VuU9F`, left in
place there). They were re-saved above under `sm_project_gsfa` and forgetting the
`sm_project_default` originals was attempted for all 10 — 9 failed with "no matching
memory" (see limitation note above) and are stale orphans left behind in
`sm_project_default`; this is harmless since nothing queries that tag for GSFA content
going forward. Do not re-attempt cleaning them up; it's not achievable via this tool.
